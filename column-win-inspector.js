export default class ColumnWinInspector {
     constructor(column) {
          this.column = column;
     }

     inspect() {
          let player1MaxNumber = 0;
          let player2MaxNumber = 0;
          for (let i = 0; i < this.column.tokens.length; i++) {
               
          }
     }
}
